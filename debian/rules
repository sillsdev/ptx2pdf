#!/usr/bin/make -f
export DH_VERBOSE = 1

export PYBUILD_NAME=ptxprint

export PYBUILD_DISABLE_python3.8=test
export PYBUILD_DISABLE_python3.6=test

%:
	dh $@ --with python3 --buildsystem=pybuild -v 


